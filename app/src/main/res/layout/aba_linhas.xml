<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.marcosevaristo.trackusregister.fragments.AbaLinhas">

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

    <ListView
        android:id="@+id/listaLinhas"
        android:layout_marginTop="@dimen/marginTopListItens"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/listItensHeight" />

    <EditText
        android:id="@+id/etBusca"
        android:layout_width="@dimen/etBuscaWidth"
        android:layout_height="@dimen/etBuscaHeight"
        android:layout_marginLeft="10dp"
        android:layout_alignParentBottom="true"
        android:visibility="gone"
        android:focusable="true"
        android:maxLength="30"
        android:inputType="number"
        android:hint="@string/hintBuscar"
        />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_search"
        android:src="@drawable/ic_fab_search"
        android:layout_width="@dimen/fabDimen"
        android:layout_height="@dimen/fabDimen"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/fab_margin"
        android:layout_marginBottom="@dimen/fab_margin"
        app:fabSize="normal"
        />

</RelativeLayout>